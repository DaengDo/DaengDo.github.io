---
classes: wide 
title: "[http] http 전송" 
excerpt: ""

categories:
  - Network
tags:
  - [Network]

toc: true
toc_sticky: true
show_date: true
read_time: false

date: 2021-12-17
last_modified_at: 2021-12-17
---

# http 메시지로 전송하는 것
- HTML, TEXT
- 이미지, 음성, 영상 파일
- JSON, XML (API)
- 거의 모든 형태의 데이터 전송 가능
- 서버간 데이터 송수신도 대부분 HTTP 사용

## 기반 프로토콜
- <b>TCP</b>: HTTP/1.1, HTTP/2
- <b>UDP</b>: HTTP/3
- 현재 HTTP/1.1 주로 사용

## HTTP 특징
- 클라이언트 서버 구조
- 무상태 프로토콜(stateless), 비연결성
- HTTP 메시지
- 단순함, 확장  가능

# 클라이언트 서버 구조
- Request, Response 구조
- 클라이언트는 서버에 요청을 보내고, 응답을 대기
- 서버가 요청에 대한 결과를 만들어 응답

# 무상태 프로토콜(Stateless)
- 서버가 클라이언트의 상태를 보존하지 않음
- 장점: 서버 확장성 높음(스케일 아웃)
  - 상태 유지
    - 중간에 context가 바뀌면 안됨
  - 무상태
    - 응답 서버를 쉽게 바꿀 수 있음 <BR>-> 무한한 서버 증설 가능 - 수평 확장 유리(스케일 아웃)
- 단점
  - 클라이언트가 많은 데이터를 전송해야함
  - 모든 것을 무상태로 설계 할 수 있는 경우도 있고 없는 경우도 있다.
    - 무상태 예시) 로그인이 필요 없는 단순 소개 화면
    - 상태 유지 예시) 로그인
    - 로그인한 사용자의 경우 로그인 했다는 상태를 서버에 유지
    - 일반적으로 브라우저 쿠키와 서버 세션등을 사용해 상태 유지
  - 따라서 상태 유지는 최소한만 사용

