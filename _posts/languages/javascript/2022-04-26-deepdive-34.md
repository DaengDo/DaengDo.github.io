---
classes: wide
title: "[DeepDive] chapter 34. 이터러블"
excerpt: "" 
categories:
  - JavaScript
tags:
  - [JS, Books]

toc: true
toc_sticky: true
show_date: true
read_time: false

date: 2022-04-26
last_modified_at: 2022-04-26
---

# 이터레이션 프로토콜

ES6 이전 자바스크립트는 순회하는 요소를 `for`, `for...in`, `forEach` 등 통일된 규칙 없이 사용 중 이었다.
이터레이션 프로토콜(ES5)이 등장하면서 순회 가능한 자료구조를 `for...of`, 스프레드, 구조 분해 할당으로 데이터 사용을 일원화했다.

# 이터러블과 이터레이터

순회 가능한 자료 구조 -> 이터러블

이터러블이 되려면 지켜야하는 규칙(`Symbol.iterator` 구현) -> 이터러블 프로토콜

이터러블이 `Symbol.iterator` 메서드를 호출 -> 이터레이터 반환

이터레이터의 `next` 메서드 호출 -> 이터러블 리절트 객체 반환

이터러블 프로토콜을 준수한 객체를 이터러블, 이터레이터 프로토콜을 준수한 객체를 이터레이터라고 한다.

```js
// 예제 34-2 - 이터러블 기본 예제
const array = [1, 2, 3];

// 배열은 Array.prototype의 Symbol.iterator 메서드를 상속받는 이터러블이다.
console.log(Symbol.iterator in array); // true

/* for(변수선언문 of 이터러블){ ... }*/
// 이터러블인 배열은 for...of 문으로 순회 가능하다.
for (const item of array) {
  console.log(item);
}

// 이터러블인 배열은 스프레드 문법의 대상으로 사용할 수 있다.
console.log([...array]); // [1, 2, 3]

// 이터러블인 배열은 배열 디스트럭처링 할당의 대상으로 사용할 수 있다.
const [a, ...rest] = array;
console.log(a, rest); // 1, [2, 3]
```

```js
// 예제 34-5 - 이터레이터 기본 예제
// 배열은 이터러블 프로토콜을 준수한 이터러블이다.
const array = [1, 2, 3];

// Symbol.iterator 메서드는 이터레이터를 반환한다.
const iterator = array[Symbol.iterator]();

// Symbol.iterator 메서드가 반환한 이터레이터는 next 메서드를 갖는다.
console.log('next' in iterator); // true
```

유사 배열 객체는 객체이기 때문에 `Symbol.iterator`를 프로토타입 체인으로 상속받지 못해 이터러블이 아닙니다.

for of, 스프레드, 구조 분해 할당을 이용하려면 `Array.from` 혹은 직접 `Symbol.iterator` 메서드를 구현하여 이터러블 프로토콜을 지키게 해줘야 합니다.

```js
// 예제 34-11 - Array.from
// 유사 배열 객체
const arrayLike = {
  0: 1,
  1: 2,
  2: 3,
  length: 3
};

// Array.from은 유사 배열 객체 또는 이터러블을 배열로 변환한다
const arr = Array.from(arrayLike);
console.log(arr); // [1, 2, 3]
```

```js
// 예제 34-12 - Symbol.iterator 직접 구현
// 피보나치 수열을 구현한 사용자 정의 이터러블
const fibonacci = {
  // Symbol.iterator 메서드를 구현하여 이터러블 프로토콜을 준수한다.
  [Symbol.iterator]() {
    let [pre, cur] = [0, 1]; // "36.1. 배열 디스트럭처링 할당" 참고
    const max = 10; // 수열의 최대값

    // Symbol.iterator 메서드는 next 메서드를 소유한 이터레이터를 반환해야 하고
    // next 메서드는 이터레이터 리절트 객체를 반환해야 한다.
    return {
      next() {
        [pre, cur] = [cur, pre + cur]; // "36.1. 배열 디스트럭처링 할당" 참고
        // 이터레이터 리절트 객체를 반환한다.
        return { value: cur, done: cur >= max };
      }
    };
  }
};

// 이터러블인 fibonacci 객체를 순회할 때마다 next 메서드가 호출된다.
for (const num of fibonacci) {
  console.log(num); // 1 2 3 5 8
}
```

# 활용

무한 이터러블을 통해서 지연 평가를 구현하는데 활용이 됩니다.

```js
const 사용자가연산떠안기 = function() {
  let cur = 0;

  return {
    [Symbol.iterator]() { return this; },
    next() {
      cur = ++cur;
      return {value: cur};
    }
  }
}

// 이렇게 쓰거나
for(const num of 사용자가연산떠안기()) {
  if(num > 20) break;
  console.log(num);
}

// 저렇게 씀
const [v1, v2, v3] = 사용자가연산떠안기();
console.log(v1, v2, v3);
```

서버 연산 부담을 고객에게 전가합시다!