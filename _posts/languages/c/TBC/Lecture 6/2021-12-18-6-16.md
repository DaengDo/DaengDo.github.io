---
classes: wide
title: "[따배씨 6-16] 배열과 런타임 에러"
excerpt: ""
categories:
  - C
tags:
  - [따배씨]
toc: false
toc_sticky: true
show_date: true
read_time: false
date: 2021-12-18
last_modified_at: 2021-12-19 # 수정한 날짜
---

- 런타임 에러
  - 문법 오류가 아니라 컴파일 환경에서는 에러를 찾지 못하지만,<BR>런타임(실행시) 생기는 오류

```c
#include <stdio.h>

// 기호적 상수, 매크로 라고 부름
#define NUM_DAYS 365

int main()
{
  char my_chars[] = "Hello, World!";

  // int array의 사이즈를 365개로 잡음
  int daily_temperature[NUM_DAYS];
  double stock_prices_history[NUM_DAYS];

  printf("%zd\n", sizeof(stock_prices_history));
  printf("%zd\n", sizeof(double) * NUM_DAYS);
  printf("%zd\n", sizeof(stock_prices_history[0]));

  // ----------------------

  int my_numbers[5];

  my_numbers[0] = 1;    // subscripts, indices, offsets
  my_numbers[1] = 3;
  my_numbers[2] = 4;
  my_numbers[3] = 2;
  my_numbers[4] = 1024;

  // scanf("%d", &my_numbers[0]);
  // 위와 아래줄은 똑같이 작동함
  scanf("%d", my_numbers);
  // my_numbers 자체가 배열의 첫번째 요소의 주소값이다.

  printf("%d\n", my_numbers[0]);
  printf("%d\n", my_numbers[1]);
  printf("%d\n", my_numbers[2]);
  printf("%d\n", my_numbers[3]);
  printf("%d\n", my_numbers[4]);

  /* Runtime Error */

  // my_numbers[5] = 123;  // out of bound
  // my_numbers = 8;       // compile error

  return 0;
}
```